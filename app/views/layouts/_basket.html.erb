<%= link_to "#{active_order.order_items.size} Items in Cart ( #{if active_order.order_items.size > 0 then number_to_currency active_order.total else number_to_currency 0 end } )", basket_path, class: "btn btn-link" %>